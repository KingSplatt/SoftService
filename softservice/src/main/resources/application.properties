spring.application.name=softportal

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/softservice}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Disneyxa}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha


# === Configuración SMTP para envío de correos (Gmail) ===
# Use variables de entorno para credenciales reales: MAIL_USERNAME y MAIL_PASSWORD
# Para cuentas de Google: genere una App Password y úsela como MAIL_PASSWORD.
# Ejemplo (Windows PowerShell):
#   setx MAIL_USERNAME "tu_email@gmail.com"
#   setx MAIL_PASSWORD "tu_app_password"

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=miguelpruebalopezprueba@gmail.com
spring.mail.password=azwo hkyu xzun uvdb
spring.mail.protocol=smtp
spring.mail.default-encoding=UTF-8

# Propiedades SMTP
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Evitar probar la conexión en arranque en entornos donde no haya credenciales
spring.mail.test-connection=false


